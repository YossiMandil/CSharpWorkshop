namespace CustomList
{
    /// <summary>
    /// Write a CustomList class the has the following methods:
    /// 1. Append - Get a item as an input and adds the item to the end of the list
    /// 2. Remove - Gets a item and remove it from the list
    /// 3. Indexer - read about it
    /// 3. Count property - Gets the size of the list
    /// 
    /// Your list should be efficient in memory.
    /// You have to use only in array.
    /// </summary>

    public class MyList
    {
        List<string> list;
    }
}